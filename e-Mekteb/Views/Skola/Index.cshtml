@model IEnumerable<e_Mekteb.Models.Skola>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AplicationUserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NazivSkole)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AplicationUserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NazivSkole)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SkolaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SkolaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SkolaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
